es:
  config:
    apiKey:
    serverUrl:

logging:
  level:
    co.elastic.clients: INFO
    org.elasticsearch.client: INFO
    org.springframework.web: INFO

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    url: /openapi.yaml
